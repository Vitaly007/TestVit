# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestVit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestVit
  #pod 'SwiftyJSON'
  pod 'Alamofire', '4.5.1' #314
  pod 'Material' , '~> 2.0'
  #pod 'Material', :git => 'https://github.com/CosmicMind/Material.git', :branch => 'development'
  #pod 'Material', :git => 'https://github.com/CosmicMind/Material.git', :branch => 'swift-2.3' 
  platform :ios, '11.0'
  source 'https://github.com/CocoaPods/Specs.git'

  target 'TestVitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestVitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
